import java.util.Stack;

public class LinkedListEx2 {
    /**
     * Doing Ex4 in assignment 2 by Linked lists of stacks
     */

    private LinkedList<Stack> LS; // linked list of stacks

    public LinkedListEx2() {
        LS = new LinkedList<Stack>();
    }

    public void removeRange(double range) {
        for (int i = 0; i < LS.size(); i++) { // O(10)
            if (i == (int) range) {

            }
        }
    }

}
